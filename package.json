{
  "name": "scifact-deploy",
  "version": "1.0.0",
  "description": "---",
  "main": "index.js",
  "scripts": {
    "install": "npm install",
    "build": "tsc",
    "build:watch": "watch 'npm run build' ."
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/exec": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^14.14.20",
    "typescript": "^4.1.3"
  }
}
